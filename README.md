# lig-examination

## Requirements
* create-react-app
* Node
* npm

## Steps
* Clone repository
* cd to project folder
* Run `npm install`
* After successful, run `npm start`

## Technologies used
* React